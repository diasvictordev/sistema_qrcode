<!DOCTYPE html>
<html>
  <%- include('./partials/head', { page: 'login', pageName: 'Login' }) %>
  <body>
    <main>
      <form action="/login" method="POST">
        

        <label for="username">CPF</label>
        <input placeholder="Digite seu CPF aqui" type="text" name="username" id="username" />
        <label for="password">Senha</label>
        <input placeholder="Digite sua senha aqui" type="password" name="password" id="password" />

        <div class="form-group">
          <input type="checkbox" name="remember" id="remember"> Lembre-me
        </div>

        <% if(error) { %>
          <div class="error">
            <p><%= error %></p>
          </div>
        <% } %>
        <button type="submit">Logar</button>
        <div class="line-or">
          <hr><p>ou</p><hr>
        </div>
        <p><a href="/register">Cadastre-se aqui</a></p>
      </form>
    </main>
  </body>
</html>
